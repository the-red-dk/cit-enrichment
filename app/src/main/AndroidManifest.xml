<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CITStudentEnrichment">

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".RegisterActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".PolicyInfoActivity" />
        <activity android:name=".ApplyActivity" />
        <activity
            android:name=".NptelFormActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ClaimStatusActivity" />
        <activity android:name=".ClaimDetailActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".NotificationsActivity" />

    </application>

</manifest>
